{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "types": [
      "vite/client"
    ],
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "paths": {
      "$/*": [
        "../../*"
      ]
    }
  },
  "include": [
    "src/**/*.ts"
  ]
}
